<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Revulsions Time</title>
  <style>
    html, body {
      margin: 0;
      padding: 0;
      width: 100%;
      height: 55%;
      font-family: serif;
      color: black;
      overflow: hidden;
    }

    body {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      font-size: 2.5rem;
      background: url('rev.png') no-repeat center center fixed;
      background-size: cover;
    }

    #dateDisplay {
      z-index: 2;
      text-shadow: 2px 2px 5px black;
    }

    #adminPanel {
      display: none;
      background: rgba(0,0,0,0.7);
      border: 1px solid #555;
      padding: 15px;
      margin-top: 20px;
      font-size: 1rem;
      z-index: 3;
    }

    #adminPanel input, #adminPanel select, #adminPanel button {
      margin: 5px 0;
      width: 100%;
    }

    #pausedStatus {
      margin-top: 10px;
      font-size: 1rem;
      color: #f90;
    }
  </style>
</head>
<body>

  <div id="dateDisplay">Loading...</div>

  <div id="adminPanel">
    <strong>Admin Panel</strong><br><br>

    <button onclick="togglePause()">Pause / Resume</button>
    <div id="pausedStatus">Running</div>

    <label>Day:</label>
    <input type="number" id="adminDay" min="1">

    <label>Month:</label>
    <select id="adminMonth"></select>

    <label>Year:</label>
    <input type="number" id="adminYear" min="1">

    <button onclick="applyDate()">Apply Date</button>

    <label>Skip X days:</label>
    <input type="number" id="skipDays" min="1" value="1">
    <button onclick="skipDays()">Skip Days</button>

    <button onclick="resetDate()">Reset Date</button>
    <button onclick="hideAdminPanel()">Hide Admin Panel</button>

    <label>Change Background Image:</label>
    <input type="file" id="bgImageInput" accept="image/*">
  </div>

  <script src="date.js"></script>
</body>
</html>
